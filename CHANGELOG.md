2.0.0:

1. **架构重构 - 事件总线 (EventBus)**：引入线程安全的发布/订阅事件总线，定义 6 种事件类型（进程状态变更、账户登录检测、账户恢复完成、密钥备份完成、应用完成、应用关闭），彻底消除所有轮询模式，改为纯事件驱动架构。
2. **架构重构 - 依赖注入 (DI)**：CLIController、Logger、ConfigService、TelegramKeyManager 均通过回调/Provider 注入外部依赖，实现 core 层零 UI 依赖，业务逻辑与界面展示完全解耦。
3. **配置系统重构**：将单一 ConfigService 拆分为 6 个职责明确的模块（PathConfig 路径常量、ConfigData 轻量读取层、ConfigField 描述符、RuntimeState 运行时状态、ConfigService 门面类、ConfigStorage 持久化层），引入原子写入、后台自动保存线程和批量更新机制。
4. **进程监控升级**：基于 watchdog 文件监听 + EventBus 事件驱动重构后台监控，使用 `threading.Event` 替代轮询等待进程启动和完成，零轮询设计提升响应速度。
5. **账户服务拆分**：新增 AccountFileSystemService（文件操作）和 AccountRecoveryService（异常恢复），职责分离提升可维护性。
6. **UI 层优化**：新增 adapters.py 适配器层作为 core 与 ui 的桥梁，统一弹窗管理器（Popup 单例），新增 AccountListModel 数据驱动列表、TaskRunner 异步任务执行等组件。
7. **双模切换**：支持使用 `-k/--key-login` 参数绕过物理文件夹匹配限制，直接将保存后的 key 编码还原写入对应文件实现登录。
8. **单体标签管理**：新增 `-t/--tag` 参数，支持对指定标签账户执行加密/解密操作。
9. **设置页 UI 重构**：数据驱动列表实现纯净短标签显示及实时 `[默认]` 标记渲染，引入强制必填表单拦截与随时取消(Cancel)的防丢机制。
10. **扫库引擎加固**：新增首次账户扫描解密授权，引入 tas_tag 指纹系统精准识别本地目录，修复因已加密档案导致的 TDF 魔数解析崩溃。
11. **切换引擎容错自愈**：修复 AccountSwitcher 内部重试漏洞，新增因异常断电引发的关联临时文件夹损失自愈能力。
12. **进程与监控改良**：修复自动启动及超时竞态错误，深度清理残留进程，拦截进入 Settings 或 Help 界面时的监控器误唤醒。
13. **退出与恢复逻辑优化**：修复正常退出时误触发恢复的问题，完善异常路径下的清理与回滚一致性。
14. **基础设施优化**：重组配置管理引入弱引用和批量更新机制，增强 UAC 高权限预警，规范化工具类底层异常接管，保障日志分隔排版。
15. **稳定性修复**：修复 TDF 文件读取报错路径、settings 解析字段命名与未知块异常类型，提升错误定位与可预期性。
16. **项目结构重组**：`src/modules/` 重命名为 `src/core/`。

1.3.0:

1. 新增双模切换：支持使用 -k/--key-login 参数绕过物理文件夹匹配限制，直接将保存后的key编码还原写入对应的文件实现登录。
2. 重构设置页UI：重构底层支持数据驱动列表，实现纯净的短标签显示及实时的 [默认] 标记渲染；引入强制必填表单拦截与随时取消(
   Cancel)的防丢机制。
3. 扫库引擎加固：新增首次账户扫描解密授权，引入 tas_tag 指纹系统精准识别本地目录，彻底修复因遇到已加密档案而导致的 TDF
   魔数解析崩溃。
4. 进程与监控改良：修复自动启动及超时竞态错误；深度清理残留进程；拦截进入 Settings 或 Help 界面时的监控器误唤醒。
5. 切换引擎容错自愈：修复 AccountSwitcher 内部的重试漏洞，新增能够自动恢复因异常断电引发的关联临时文件夹损失自愈能力；新增支持
   -t/--tag 参数进行单体标签管理。
6. 基础设施优化：重组 ConfigManage 引入弱引用和批量更新机制，增强 UAC 高权限预警，规范化工具类底层异常接管，同时保障日志分隔排版。
7. 切换流程优化：切换后的状态监控改为后台线程执行，避免阻塞主流程并提升界面响应速度。
8. 退出与恢复逻辑优化：修复正常退出时误触发恢复的问题，完善异常路径下的清理与回滚一致性。
9. 稳定性修复：修复 TDF 文件读取报错路径、settings 解析字段命名与未知块异常类型，提升错误定位与可预期性。

1.2.0:

1. 新增标签账户加密功能
2. 优化代码逻辑
3. 优化项目结构
4. 删除冗余方法

1.1.4:

1. 优化客户端搜索逻辑
2. 将客户端搜索按钮和路径搜索按钮合并为一个按钮
3. 将日志记录器从logging更换为loguru
4. 优化客户端监控逻辑
5. 优化配置管理器
6. 删除无用代码, 优化项目架构
